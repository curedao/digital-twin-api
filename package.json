{
  "name": "digital-twin-api",
  "version": "1.0.0",
  "description": "Digital Twin Application Programming Interface (DT-API)",
  "main": "express/index.js",
  "scripts": {
    "install": "cd server && npm install && cd ../client && npm install && cd ../express && npm install",
    "prisma:generate": "cd server/prisma && npx prisma db pull",
    "models:generate": "npx ts-node server/scripts/generateModels.ts"
  },
  "keywords": [
    "digital health"
  ],
  "author": "mikepsinn",
  "license": "GPL-3.0-or-later",
  "dependencies": {

  }
}
